- content_for(:title) do
  %h1= t('scaffold.actions.edit', :item => <%= singular_table_name %>).titlecase

= render 'form'

= link_to t('scaffold.actions.show').titlecase, @<%= singular_table_name %>
\|
= link_to t('scaffold.actions.back').titlecase, <%= index_helper %>_path

